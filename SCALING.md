# 规模预期与架构决策：为什么不用数据库

> 结论先行：**个人记忆插件用 json 存储领域足够，不上数据库。** 依据是 1 万条真实库的实测数据，不是估算。

## 背景

`dsh-echo-memory` 开源后，不能只按维护者个人的记忆量（几百条）来评估架构。本文档回答两个问题：

1. 数据量涨上去，json 是否扛得住？
2. 为什么架构上**不引入数据库**（SQLite / LevelDB / 其他索引式后端）？

## 实测方法

在隔离环境构造**真实 json 存储文件（10,000 条记录，4.55 MB）**，用真实 `JsonStorageBackend` + 构建产物 `MemoryStore` 跑四类操作：

- 启动加载：backend.open + zod 逐条校验 10,000 条
- 注入候选：`rankedForInjection`——**每轮模型请求都会跑**的最高频路径（100 次取均值）
- 关键词检索：`memory_search`（含字符串运算，20 次取均值）
- 单次保存：`memory_save`——含 4.55 MB 全文件原子重写 + fsync

## 实测数据（10,000 条 / 4.55 MB）

| 操作 | 耗时 | 频率 |
| --- | --- | --- |
| 启动加载（open + zod 校验 1 万条） | **65 ms** | 每次启动一次 |
| 注入候选（每轮模型请求） | **1.74 ms** | 最高频，每次请求 |
| 关键词检索 | **4.3 ms** | agent 显式调用 |
| 单次保存（全文件原子重写） | **45 ms** | 用户/agent 显式触发 |

万级库全路径无感；对 LLM 工具调用（几百 ms 量级）与对话组装而言完全不可感知。

## 外推（线性增长）

| 规模 | 文件大小 | 注入候选 | 单次保存 | 启动加载 |
| --- | --- | --- | --- | --- |
| 10,000（实测） | 4.55 MB | 1.7 ms | 45 ms | 65 ms |
| 50,000 | ~23 MB | ~9 ms | ~0.2 s | ~0.3 s |
| 100,000 | ~46 MB | ~17 ms | ~0.5 s | ~0.65 s |

个人使用存到 10,000 条意味着每天存 20 条也要存一年半；100,000 条约等于每天 50 条连存五年——远远超出个人记忆插件的真实负载。

## 架构决策：不上数据库

**1. 实测余量超过 10 倍。** 真实负载在千级，实测万级全路径无感，余量两个数量级。

**2. 换数据库的成本是真实的。** SQLite/LevelDB 类后端意味着：

- 引入**原生依赖**（跨平台二进制、构建链、`allowBuilds` 授权、安装体积）；
- 数据迁移（存量 memory.json → 新后端，schema 与版本管理复杂度）；
- 失去 json 的**人类可读**特性（直接 cat / 编辑 / 备份 / 迁移）。
- 换来的是"个人用户几乎到不了的规模"上的性能——性价比为负。

**3. json 是特性，不是妥协。** 零依赖、装完即用、文件即数据——这正是本插件区别于通用记忆服务的定位。

**4. 风险已管理，信号线明确。** 如果未来真实用户报告性能问题，信号线是：

- 记忆量达到**数万条**或文件超过 **20 MB**——单次保存开始有明显延迟（~0.2 s）；
- 达到后，决策升级为：引入索引式后端（同时解决检索 O(n) 与写入放大），并与语义召回（embedding 检索）一并规划。

在那之前，**不做架构预支**：不为用不到的规模买单。